import { query } from './_lib/db.js'; import { handleError,json,requireAccess } from './_lib/http.js';
export default async function handler(req:any,res:any){if(req.method!=='GET')return json(res,405,{error:'Method not allowed'});if(!requireAccess(req,res))return;try{const rows=await query(`select a.id,a.attendance_date,a.status,a.late_minutes,a.points_penalty,coalesce(s.full_name,u.full_name,'بدون اسم') full_name,c.name circle_name from attendance a join students s on s.id=a.student_id left join users u on u.id=s.user_id left join circles c on c.id=a.circle_id order by a.attendance_date desc,a.created_at desc limit 300`);return json(res,200,{items:rows})}catch(e){return handleError(res,e)}}