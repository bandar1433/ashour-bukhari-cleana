import { query } from './_lib/db.js'; import { handleError,json,requireAccess } from './_lib/http.js';
export default async function handler(req:any,res:any){if(!requireAccess(req,res))return;try{
if(req.method==='GET'){const rows=await query(`select a.id,a.attendance_date,a.status,a.late_minutes,a.points_penalty,coalesce(s.full_name,u.full_name,'بدون اسم') full_name,c.name circle_name from attendance a join students s on s.id=a.student_id left join users u on u.id=s.user_id left join circles c on c.id=a.circle_id order by a.attendance_date desc,a.created_at desc limit 300`);return json(res,200,{items:rows})}
if(req.method==='POST'){const b=req.body||{};const s=(await query<any>('select id,circle_id from students where id=$1',[b.student_id]))[0];if(!s?.circle_id)return json(res,400,{error:'الطالب غير مرتبط بحلقة'});const rows=await query(`insert into attendance(student_id,circle_id,attendance_date,status,late_minutes,points_penalty,notes) values($1,$2,$3,$4,$5,$6,$7) on conflict(student_id,attendance_date) do update set status=excluded.status,late_minutes=excluded.late_minutes,points_penalty=excluded.points_penalty,notes=excluded.notes returning *`,[b.student_id,s.circle_id,b.attendance_date,b.status||'present',Number(b.late_minutes||0),Number(b.points_penalty||0),b.notes||null]);return json(res,200,rows[0])}
return json(res,405,{error:'Method not allowed'});}catch(e){return handleError(res,e)}}