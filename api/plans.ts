import { query } from './_lib/db.js'; import { handleError,json,requireAccess } from './_lib/http.js';
export default async function handler(req:any,res:any){if(req.method!=='GET')return json(res,405,{error:'Method not allowed'});if(!requireAccess(req,res))return;try{const rows=await query(`select w.id,w.week_start,w.day_name,w.new_target,w.review_target,w.goals,coalesce(s.full_name,u.full_name,'بدون اسم') full_name from weekly_plans w join students s on s.id=w.student_id left join users u on u.id=s.user_id order by w.week_start desc,w.created_at desc limit 300`);return json(res,200,{items:rows})}catch(e){return handleError(res,e)}}